---
  imports:
    # Core
    - "../../core/color-palette.alias.yml"
    - "../../core/core.alias.yml"

    # Uplift
    - "../color-palette.alias.yml"
    - "../uplift.alias.yml"

  global:
    category: "buttons"

  props:
    # Buttons
    button-height:
      value: "38px"
      type: "size"
    button-font-weight:
      value: "{!FONT_WEIGHT_BOLD}"
      type: "font-weight"
    button-line-height:
      value: "1"
      type: "line-height"
