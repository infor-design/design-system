---
  imports:
    - "../core.alias.yml"

  global:
    category: "lists"

  props:
    ## List Colors
    list-hover-color:
      value: "{!PALETTE_GRAPHITE_2}"
      type: "color"
    list-selected-color:
      value: "{!PALETTE_GRAPHITE_1}"
      type: "color"
