---
  imports:
    # Extend Core Aliases (includes core color_palette)
    - "../core/core.alias.yml"

  aliases:
    # Borders
    BORDER_COLOR: "{!PALETTE_SLATE_4}"

    # Box shadow
    BOX_SHADOW_COLOR: "rgba(0, 0, 0, 0.4)"
