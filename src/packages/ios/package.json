{
  "private": true,
  "name": "@infor/ids-ios-tokens",
  "version": "1.0.0-alpha.1",
  "description": "Infor Design System Design Tokens",
  "scripts": {
    "build": "npm run clean && theo ./ids-ios-tokens.yml --format ios.json,raw.json --dest ./dist",
    "clean": "del dist/*",
    "prepublishOnly": "npm test && npm run build",
    "test": "yamllint ./*.yml"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/infor-design/design-system/blob/master/README.md"
  },
  "keywords": [
    "design tokens",
    "infor design system",
    "ios"
  ],
  "author": "Hook & Loop",
  "license": "UNLICENSED",
  "devDependencies": {
    "del-cli": "^1.1.0",
    "theo": "^6.0.0-beta.9",
    "yaml-lint": "^1.0.0"
  },
  "files": [
    "dist"
  ]
}
